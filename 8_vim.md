```bash
vim filename.txt

# Start typing
i
# Insert Mode (typing cmd) ⇒ Normal Mode (executing cmd)
esc

# Forward search
/your-search
# Backward Search
?your-search

# Navigating search result
n ⇒ jump next
N ⇒ jump previous
```